# Postgres — used by both docker-compose and the server
POSTGRES_USER=swell
POSTGRES_PASSWORD=swell
POSTGRES_DB=swell_engine

# Server
DATABASE_URL=postgres://swell:swell@localhost:5432/swell_engine
REDIS_URL=redis://localhost:6379
PORT=3000
NDBC_DATA_TTL_HOURS=6
